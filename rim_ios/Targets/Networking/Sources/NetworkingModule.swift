import Foundation

/// Placeholder marker for the `Networking` module.
///
/// Populated with the `APIClient` (URLSession + async/await) and request/response
/// plumbing by later issues. Exists now so the target and its test target compile.
public enum NetworkingModule {
    public static let name = "Networking"
}
