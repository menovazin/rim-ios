import Foundation

/// Placeholder marker for the `Features` module.
///
/// Populated with TCA reducers and SwiftUI views for every screen by later
/// issues. Exists now so the target and its test target compile.
public enum FeaturesModule {
    public static let name = "Features"
}
