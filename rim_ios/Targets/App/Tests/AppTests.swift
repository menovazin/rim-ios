import XCTest
@testable import App

final class AppTests: XCTestCase {
    func test_appModuleLoads() {
        // Smoke test: the App target and its dependencies link.
        XCTAssertTrue(true)
    }
}
