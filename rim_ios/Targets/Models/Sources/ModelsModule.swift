import Foundation

/// Placeholder marker for the `Models` module.
///
/// Populated with domain entities and `Codable` DTOs (Character, Episode,
/// Location, paginated `Info`) by later issues. Exists now so the target and
/// its test target compile.
public enum ModelsModule {
    public static let name = "Models"
}
